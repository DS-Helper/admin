# AGENTS.md

## 기본 원칙
1. 항상 공식문서 기준으로 판단한다.
2. 항상 이 프로젝트의 기존 코드와 구조를 최우선으로 따른다.
3. 사용자가 요구하지 않은 변경은 하지 않는다.
4. 요구사항이 불분명하면 수정 전에 반드시 질문한다.
5. 한글이 깨지지 않도록 주의한다.
6. 테스트 코드 실행 시에는 자율적으로 실행한다.
7. 문맥 충돌 시 줄 단위 최소 치환으로 재시도
8. 작성한 사항에 대해 단계별로 충분한 주석을 작성
9. 단계별로 충분한 디버깅용 로그를 작성
10. Git 작업(Add, Commit, Push 등)은 사용자의 명시적 요청이 있을 시 자율적으로 실행한다.

## 설계 기준
1. 모든 로직 해석의 최우선 기준은 Entity이다.
2. 타입, 필드 의미, 값의 형태는 먼저 Entity를 기준으로 판단한다.
3. Controller, DTO, Service, Repository는 Entity 기준을 따라야 한다.
4. 문서화나 Swagger 수정도 실제 Entity 및 응답 계약을 바꾸지 않는 선에서 진행한다.

## 수정 원칙
1. 요청받은 범위 안에서만 수정한다.
2. 기존 코드 스타일, 네이밍, 계층 구조를 유지한다.
3. 임의의 리팩터링, 추상화, 구조 변경을 하지 않는다.
4. 타입 변경은 실제 도메인 기준이 명확할 때만 진행한다.
5. 값의 형식과 타입을 혼동하지 않는다.
6. 테스트 코드 수정은 자율적으로 한다.
7. 전체 교체는 정말 불가피할 때만, 사전 고지 후 진행
8. 불필요한 수정은 절대 불가. 근거가 명확히 있는 수정만 가능.
9. 부분 패치 우선


## 응답/문서화 원칙
1. Swagger 문서는 실제 코드와 응답 구조를 기준으로 맞춘다.
2. Swagger를 위해 런타임 계약을 임의로 바꾸지 않는다.
3. 공통 응답 구조를 수정할 때는 전체 사용처 영향을 먼저 확인한다.
4. example, description, schema는 실제 Entity와 DTO 의미에 맞게 작성한다.
5. 도메인별 분류하여 응답한다.

## 검증 원칙
1. 수정 후 가능한 범위에서 컴파일 또는 테스트로 검증한다.
2. 검증하지 못한 경우 그 사실을 명확히 알린다.
3. 실패 시 원인과 영향 범위를 먼저 설명한다.

## 작업 태도
1. 추측보다 코드 확인을 우선한다.
2. 애매하면 일반론보다 현재 프로젝트 구현을 기준으로 답한다.
3. 사용자의 기준이 명확히 제시되면 그 기준을 이후 판단의 우선순위에 반영한다.


<!-- CODEX_GLOBAL_TOKEN_POLICY_START -->
# Codex Global Token-Minimization Policy

## 목적
- 모든 작업에서 응답/탐색/수정/검증 과정의 토큰 소모를 자동으로 최소화한다.

## 기본 규칙(항상)
- 응답: 1줄 결론 + 최대 6개 bullet. 장문 설명 금지.
- 불명확: 긴 가정 설명 대신 질문 1개로 확정.
- 코드 출력: 전체 파일/전체 함수 재출력 금지. 변경 라인만 다룬다.
- 탐색: `rg`로 후보 축소 후 필요한 파일만 부분 조회.
- 도구 호출: 독립 조회는 병렬 처리로 1회 호출, 중복 호출 금지.
- 수정: `apply_patch`로 변경 라인만. 불필요한 포맷/리네임/정리 금지.
- 검증: 관련 테스트 우선, 필요할 때만 범위 확대.
- Git: add/commit/push는 사용자의 명시적 요청이 있을 때만.

## 예외(필요 시)
- 안전/정확성/요구사항 확정에 필요할 때만 최소한으로 상세화하며, 이유 1줄을 남긴다.
<!-- CODEX_GLOBAL_TOKEN_POLICY_END -->

